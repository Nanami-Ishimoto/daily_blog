%h1 Listing posts

%table.table.table-striped
  %thead
    %tr
      %th Title
      %th Content
  %tbody
    - @posts.each do |post|
      %tr
        %td= link_to post.title, post_path(post)
        %td= post.content
